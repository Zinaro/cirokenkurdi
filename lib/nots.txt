FirebaseAuth auth = FirebaseAuth.instance;


Query blogyazilari = FirebaseFirestore
    .instance
    .collection("Yazilar")
    .where("kullaniciid", isEqualTo: auth.currentUser.uid)

    yazilar koleksiyonu içindeki belgelere bakip kullaniciid
    si verilen uid e eşit ise yazilari getirecek yani sadece istenileni

return SafeArea() //Yukaridaki saat bildirim kisminin uygulama dişinda kalmasini saglar

body: Container( // verilmese de olurmuş
    // ama verilmezse konteynir kendini içindeki widget lara gore ayarlar
    width: MediaQuery.of(context).size.width //cihazin varolan genişligini kaplar
    height: MediaQuery.of(context).size.height //cihazin varolan yuksekligini kaplar
)

SingleChildScrollView() // ekran taşmasina birebir



